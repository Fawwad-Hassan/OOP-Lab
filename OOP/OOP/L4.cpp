#include <iostream>
#include <cmath>
using namespace std;

#include "LAB4.h"

//Task 1 - SquareBox
/*int main() {

	SquareBox first;
	cout << "\nEnter lenght or width or height of square box.\n";
	int h;
	cin >> h;
	first.setHeight(h);

	SquareBox second(first);

	cout << "\nVolume of first square box is: " << first.SBvolume();
	cout << "\nVolume of second square box is: " << second.SBvolume();
	cout << "\nPerimeter of first square box is: " << first.SBperimeter();
	cout << "\nPerimeter of second square box is: " << second.SBvolume();
	return 0;
}*/


//Task 2 - HugeInteger

/*int main() {

	hugeInteger first, second, adding, zero;

	first.input();
	first.output();
	second = first;
	second.output();
	adding = first.add(second);
	adding.output();
	zero.isZero();
	first.isEqualTo(second);
	adding.isGreaterThan(first);

	return 0;
}*/


//Task 3 - random math stuff

/*int main() {

	taskThree object;
	cout << "\nRandom number generated is: " << object.randomFind();

	object.quadraticFormula();

	cout << "\nEnter angle in degrees: \n";
	float angle;
	cin >> angle;
	object.setDegrees(angle);
	object.sinCosTan(object.getDegrees());

	return 0;
}*/